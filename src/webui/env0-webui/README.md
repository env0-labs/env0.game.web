# env0-webui

Next.js (React) + Tailwind thin client for env0.game.web.

## Run

```bash
cd src/webui/env0-webui
cp -n .env.local.example .env.local
npm install
npm run dev
```

Then open http://localhost:3000
